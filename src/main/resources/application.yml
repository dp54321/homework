
#数据量连接信息
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/homework?characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  type-aliases-package: com.dp.homework.mapper
  mapper-locations: classpath:mapper/*.xml
#mybatis配置